using UnityEngine;

public class Filling : MonoBehaviour
{
    [SerializeField] public Color _redBeanColor;
    [SerializeField] public Color _custardColor;
    [SerializeField] public Color _chocolateColor;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
